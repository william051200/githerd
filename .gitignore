# User's real configuration -- never commit. Use config.example.json as a template.
config.json

# Loader temp files (in case they ever land next to the script)
githerd_loader_*.cmd

# Release build output
dist/

# Compiled fake git used by integration tests (rebuilt on demand from FakeGit.cs)
tests/Integration/FakeGit/git.exe

# Pester test output (NUnit XML)
testResults.xml
testresults.xml
