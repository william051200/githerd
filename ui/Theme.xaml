<!--
    githerd Theme.xaml
    DESIGN.md token mapping. See docs/ui-design.md for substitution notes.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- ===== Color tokens (DESIGN.md) ===== -->
    <Color x:Key="ColorCanvas">#FFFAF9F5</Color>
    <Color x:Key="ColorSurfaceCard">#FFEFE9DE</Color>
    <Color x:Key="ColorSurfaceCardSoft">#FFF4EFE6</Color>
    <Color x:Key="ColorInk">#FF141413</Color>
    <Color x:Key="ColorMuted">#FF5A544A</Color>
    <Color x:Key="ColorHairline">#FFE6DFD8</Color>
    <Color x:Key="ColorHairlineStrong">#FFD8CFC2</Color>
    <Color x:Key="ColorPrimary">#FFCC785C</Color>
    <Color x:Key="ColorPrimaryActive">#FFA9583E</Color>
    <Color x:Key="ColorPrimaryWashed">#33CC785C</Color>
    <Color x:Key="ColorOnPrimary">#FFFFFFFF</Color>
    <Color x:Key="ColorDanger">#FFB4452F</Color>
    <Color x:Key="ColorSuccess">#FF3F7A4F</Color>
    <Color x:Key="ColorSuccessWashed">#223F7A4F</Color>

    <SolidColorBrush x:Key="Canvas"          Color="{StaticResource ColorCanvas}" />
    <SolidColorBrush x:Key="SurfaceCard"     Color="{StaticResource ColorSurfaceCard}" />
    <SolidColorBrush x:Key="SurfaceCardSoft" Color="{StaticResource ColorSurfaceCardSoft}" />
    <SolidColorBrush x:Key="Ink"             Color="{StaticResource ColorInk}" />
    <SolidColorBrush x:Key="Muted"           Color="{StaticResource ColorMuted}" />
    <SolidColorBrush x:Key="Hairline"        Color="{StaticResource ColorHairline}" />
    <SolidColorBrush x:Key="HairlineStrong"  Color="{StaticResource ColorHairlineStrong}" />
    <SolidColorBrush x:Key="Primary"         Color="{StaticResource ColorPrimary}" />
    <SolidColorBrush x:Key="PrimaryActive"   Color="{StaticResource ColorPrimaryActive}" />
    <SolidColorBrush x:Key="PrimaryWashed"   Color="{StaticResource ColorPrimaryWashed}" />
    <SolidColorBrush x:Key="OnPrimary"       Color="{StaticResource ColorOnPrimary}" />
    <SolidColorBrush x:Key="Danger"          Color="{StaticResource ColorDanger}" />
    <SolidColorBrush x:Key="Success"         Color="{StaticResource ColorSuccess}" />
    <SolidColorBrush x:Key="SuccessWashed"   Color="{StaticResource ColorSuccessWashed}" />

    <!-- ===== Typography ===== -->
    <!-- Copernicus serif → Cambria fallback. StyreneB sans → Segoe UI. -->
    <FontFamily x:Key="FontDisplay">Cambria, Georgia, 'Times New Roman', serif</FontFamily>
    <FontFamily x:Key="FontBody">Segoe UI, Arial, sans-serif</FontFamily>
    <FontFamily x:Key="FontMono">Consolas, 'Courier New', monospace</FontFamily>

    <!-- ===== Geometry tokens ===== -->
    <CornerRadius x:Key="RadiusInput">8</CornerRadius>
    <CornerRadius x:Key="RadiusCard">12</CornerRadius>
    <CornerRadius x:Key="RadiusPill">999</CornerRadius>
    <Thickness    x:Key="HairlineThickness">1</Thickness>

    <!-- ===== Window background default ===== -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Canvas}" />
        <Setter Property="FontFamily" Value="{StaticResource FontBody}" />
        <Setter Property="FontSize"   Value="14" />
        <Setter Property="Foreground" Value="{StaticResource Ink}" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="TextOptions.TextRenderingMode"  Value="ClearType" />
    </Style>

    <!-- ===== Display headline (page title) ===== -->
    <Style x:Key="DisplayTitle" TargetType="TextBlock">
        <Setter Property="FontFamily"     Value="{StaticResource FontDisplay}" />
        <Setter Property="FontSize"       Value="28" />
        <Setter Property="FontWeight"     Value="SemiBold" />
        <Setter Property="Foreground"     Value="{StaticResource Ink}" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal" />
    </Style>

    <Style x:Key="Subhead" TargetType="TextBlock">
        <Setter Property="FontSize"   Value="13" />
        <Setter Property="Foreground" Value="{StaticResource Muted}" />
    </Style>

    <Style x:Key="SectionLabel" TargetType="TextBlock">
        <Setter Property="FontSize"   Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource Muted}" />
        <Setter Property="Margin"     Value="0,0,0,6" />
    </Style>

    <Style x:Key="FieldLabel" TargetType="TextBlock">
        <Setter Property="FontSize"   Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource Ink}" />
        <Setter Property="Margin"     Value="0,0,0,4" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="Hint" TargetType="TextBlock">
        <Setter Property="FontSize"   Value="11" />
        <Setter Property="Foreground" Value="{StaticResource Muted}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <!-- ===== TextBox: text-input ===== -->
    <Style x:Key="TextBoxStyle" TargetType="TextBox">
        <Setter Property="Background"      Value="{StaticResource Canvas}" />
        <Setter Property="Foreground"      Value="{StaticResource Ink}" />
        <Setter Property="BorderBrush"     Value="{StaticResource Hairline}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding"         Value="10,6" />
        <Setter Property="MinHeight"       Value="32" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontSize"        Value="13" />
        <Setter Property="CaretBrush"      Value="{StaticResource Primary}" />
        <Setter Property="SelectionBrush"  Value="{StaticResource PrimaryWashed}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <Border x:Name="OuterRing"
                                CornerRadius="10"
                                BorderThickness="3"
                                Margin="-2"
                                BorderBrush="Transparent" />
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Primary}" />
                            <Setter TargetName="Bd" Property="BorderThickness" Value="1.5" />
                            <Setter TargetName="OuterRing" Property="BorderBrush" Value="{StaticResource PrimaryWashed}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource HairlineStrong}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Multiline variant (final command) -->
    <Style x:Key="TextAreaStyle" TargetType="TextBox" BasedOn="{StaticResource TextBoxStyle}">
        <Setter Property="FontFamily"            Value="{StaticResource FontMono}" />
        <Setter Property="FontSize"              Value="12" />
        <Setter Property="AcceptsReturn"         Value="True" />
        <Setter Property="TextWrapping"          Value="Wrap" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="Padding"               Value="10,8" />
        <Setter Property="MinHeight"             Value="64" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
    </Style>

    <!-- ===== Buttons ===== -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background"      Value="{StaticResource Primary}" />
        <Setter Property="Foreground"      Value="{StaticResource OnPrimary}" />
        <Setter Property="BorderBrush"     Value="{StaticResource Primary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding"         Value="16,7" />
        <Setter Property="MinHeight"       Value="34" />
        <Setter Property="MinWidth"        Value="80" />
        <Setter Property="FontSize"        Value="13" />
        <Setter Property="FontWeight"      Value="SemiBold" />
        <Setter Property="Cursor"          Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background"  Value="{StaticResource PrimaryActive}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryActive}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background"  Value="{StaticResource PrimaryActive}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="Background"      Value="{StaticResource Canvas}" />
        <Setter Property="Foreground"      Value="{StaticResource Ink}" />
        <Setter Property="BorderBrush"     Value="{StaticResource HairlineStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding"         Value="16,7" />
        <Setter Property="MinHeight"       Value="34" />
        <Setter Property="MinWidth"        Value="80" />
        <Setter Property="FontSize"        Value="13" />
        <Setter Property="FontWeight"      Value="SemiBold" />
        <Setter Property="Cursor"          Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceCardSoft}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Primary}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceCard}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Compact secondary, used for Add / Remove / Browse -->
    <Style x:Key="SecondaryButtonSmall" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
        <Setter Property="Padding"   Value="12,6" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="MinWidth"  Value="0" />
        <Setter Property="FontSize"  Value="13" />
    </Style>

    <Style x:Key="DangerButtonSmall" TargetType="Button" BasedOn="{StaticResource SecondaryButtonSmall}">
        <Setter Property="Foreground" Value="{StaticResource Danger}" />
    </Style>

    <!-- ===== CheckBox ===== -->
    <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Ink}" />
        <Setter Property="FontSize"   Value="13" />
        <Setter Property="Cursor"     Value="Hand" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="Box" Grid.Column="0"
                                Width="18" Height="18"
                                CornerRadius="4"
                                Background="{StaticResource Canvas}"
                                BorderBrush="{StaticResource HairlineStrong}"
                                BorderThickness="1.5"
                                VerticalAlignment="Top"
                                Margin="0,1,0,0">
                            <Path x:Name="Check"
                                  Data="M 3 9 L 7 13 L 15 5"
                                  Stroke="{StaticResource OnPrimary}"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter Grid.Column="1"
                                          Margin="9,0,0,0"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True">
                            <ContentPresenter.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap" />
                                    <Setter Property="LineHeight"   Value="17" />
                                </Style>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box"   Property="Background"  Value="{StaticResource Primary}" />
                            <Setter TargetName="Box"   Property="BorderBrush" Value="{StaticResource Primary}" />
                            <Setter TargetName="Check" Property="Visibility"  Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Primary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Card container ===== -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background"      Value="{StaticResource SurfaceCard}" />
        <Setter Property="BorderBrush"     Value="{StaticResource Hairline}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius"    Value="12" />
        <Setter Property="Padding"         Value="20" />
    </Style>

    <Style x:Key="CardSoft" TargetType="Border" BasedOn="{StaticResource Card}">
        <Setter Property="Background" Value="{StaticResource Canvas}" />
    </Style>

    <!-- ===== Repo list-item card (used as ListBoxItem template) ===== -->
    <Style x:Key="RepoListBoxStyle" TargetType="ListBox">
        <Setter Property="Background"      Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility"   Value="Auto" />
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding"   Value="0" />
                    <Setter Property="Margin"    Value="0,0,0,8" />
                    <Setter Property="Cursor"    Value="Hand" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd"
                                        Background="{StaticResource Canvas}"
                                        BorderBrush="{StaticResource Hairline}"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Padding="14,10">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource HairlineStrong}" />
                                        <Setter TargetName="Bd" Property="Background"  Value="{StaticResource SurfaceCardSoft}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Primary}" />
                                        <Setter TargetName="Bd" Property="BorderThickness" Value="1.5" />
                                        <Setter TargetName="Bd" Property="Background"  Value="{StaticResource SurfaceCardSoft}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Validation banner -->
    <Style x:Key="ErrorBanner" TargetType="Border">
        <Setter Property="Background"      Value="#22CC785C" />
        <Setter Property="BorderBrush"     Value="{StaticResource Primary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius"    Value="8" />
        <Setter Property="Padding"         Value="14,10" />
    </Style>
    <Style x:Key="ErrorBannerText" TargetType="TextBlock">
        <Setter Property="Foreground"   Value="{StaticResource Danger}" />
        <Setter Property="FontSize"     Value="13" />
        <Setter Property="FontWeight"   Value="SemiBold" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <!-- Success / info banner -->
    <Style x:Key="SuccessBanner" TargetType="Border">
        <Setter Property="Background"      Value="{StaticResource SuccessWashed}" />
        <Setter Property="BorderBrush"     Value="{StaticResource Success}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius"    Value="8" />
        <Setter Property="Padding"         Value="14,10" />
    </Style>
    <Style x:Key="SuccessBannerText" TargetType="TextBlock">
        <Setter Property="Foreground"   Value="{StaticResource Success}" />
        <Setter Property="FontSize"     Value="13" />
        <Setter Property="FontWeight"   Value="SemiBold" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <!-- Thin separator -->
    <Style x:Key="HairlineSeparator" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Hairline}" />
        <Setter Property="Height"     Value="1" />
        <Setter Property="Margin"     Value="0,16" />
    </Style>

    <!-- ===== Chip badges (squarer than pills) ===== -->
    <Style x:Key="BadgePill" TargetType="Border">
        <Setter Property="Background"      Value="{StaticResource SurfaceCard}" />
        <Setter Property="BorderBrush"     Value="{StaticResource Hairline}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius"    Value="5" />
        <Setter Property="Padding"         Value="8,3" />
        <Setter Property="Margin"          Value="0,0,6,0" />
    </Style>

    <Style x:Key="BadgePillCoral" TargetType="Border" BasedOn="{StaticResource BadgePill}">
        <Setter Property="Background"  Value="{StaticResource PrimaryWashed}" />
        <Setter Property="BorderBrush" Value="{StaticResource Primary}" />
    </Style>

    <Style x:Key="BadgeText" TargetType="TextBlock">
        <Setter Property="FontSize"   Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource Ink}" />
    </Style>

    <Style x:Key="BadgeTextCoral" TargetType="TextBlock" BasedOn="{StaticResource BadgeText}">
        <Setter Property="Foreground" Value="{StaticResource PrimaryActive}" />
    </Style>

    <Style x:Key="BadgeTextMuted" TargetType="TextBlock" BasedOn="{StaticResource BadgeText}">
        <Setter Property="Foreground" Value="{StaticResource Muted}" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <!-- Italic muted style for placeholder text in the repo list -->
    <Style x:Key="PlaceholderName" TargetType="TextBlock">
        <Setter Property="FontSize"   Value="15" />
        <Setter Property="FontStyle"  Value="Italic" />
        <Setter Property="Foreground" Value="{StaticResource Muted}" />
    </Style>

</ResourceDictionary>
